name: Security Scan
description: Full history secrets scan with Gitleaks

runs:
  using: composite
  steps:
    - name: Gitleaks Scan (Full History)
      uses: gitleaks/gitleaks-action@v3
      env:
        GITHUB_TOKEN: ${{ env.GITHUB_TOKEN }}
      with:
        args: --verbose --redact
