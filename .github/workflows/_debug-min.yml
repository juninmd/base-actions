name: Debug Min

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        required: false
        default: '22'

jobs:
  noop:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - run: echo hi
