name: Reusable Security Scan

on:
  workflow_call:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0 # Important for full history scan
      - uses: juninmd/base-actions/security-scan@main
